# Pin the toolchain for reproducible builds — the same 1.96.0 the `nat` research crates
# pin (PLANSET reproducibility floor). This kernel is a committed-byte object: same source
# + same compiler -> same digests on every machine and every federated node.
[toolchain]
channel = "1.96.0"
components = ["rustfmt", "clippy"]
